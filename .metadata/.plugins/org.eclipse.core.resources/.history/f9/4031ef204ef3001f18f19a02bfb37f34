package Inventory;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

public class test_runnner {

    public static void main(String[] args) {
        // Initialize WebDriver (you may need to specify the path to the driver here)
        WebDriver driver = new ChromeDriver();

        driver.get("https://vividtranstech.com/tancem/#/login"); 

        driver.manage().window().maximize();



        Login.performLoginAndNavigate(driver);
        
        item_request.itemcreation(driver);

        //in_gate_pass_rawmaterial.initiate1(driver);
        
        // Close the browser after operations
    }
}
